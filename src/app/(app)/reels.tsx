import { Container } from '@/components/ui/container';
import { Title } from '@/components/ui/typography';

export default (): JSX.Element => (
  <Container>
    <Title>The Reels Page</Title>
  </Container>
);
